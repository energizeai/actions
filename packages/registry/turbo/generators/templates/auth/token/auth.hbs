const AuthConfig = {
  type: "token",

  button: {
    text: "Continue with {{ titleCase resource }}",
    image: {
      light: "/logos/{{ camelCase resource }}.svg",
      dark: "/logos/{{ camelCase resource }}.svg",
    },
  },

  humanReadableName: "{{ name }}",
  humanReadableDescription: "{{ scopeDescription }}",

  generatingTokenReferenceURL: "{{ generatingTokenReferenceURL }}",

  policyReferenceURL: "",

  customDataSchema: z.object({
    fieldOne: z.string().min(1).describe(`The first custom data field`),
    fieldTwo: z.string().min(1).describe(`The second custom data field`),
  }),

} as const satisfies AuthConfig;

type TAuth = typeof AuthConfig;